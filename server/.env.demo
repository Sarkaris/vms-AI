# Demo Environment Configuration
# This file configures the application for demo mode

# Demo Mode - Set to true for read-only demonstration
DEMO_MODE=true

# SQLite Demo Database (No MySQL required!)
# Uses in-memory database for instant demo setup
DEMO_DATABASE=sqlite

# JWT Secret (use a secure secret in production)
JWT_SECRET=demo-secret-key-do-not-use-in-production

# Server Configuration
PORT=5000

# Email Configuration (Optional for demo)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=demo@example.com
EMAIL_PASS=demo-password

# Security
BCRYPT_ROUNDS=10
SESSION_SECRET=demo-session-secret

# Demo Credentials (for testing)
DEMO_USERNAME=demo
DEMO_PASSWORD=demo123